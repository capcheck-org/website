FROM node:22-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm install

# Copy source
COPY . .

# Expose port
EXPOSE 4321

# Run dev server with host flag for Docker
CMD ["npm", "run", "dev:frontend", "--", "--host", "0.0.0.0"]
